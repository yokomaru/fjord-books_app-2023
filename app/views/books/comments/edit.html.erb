<h1><%= t('views.common.title_edit', name: Comment.model_name.human.downcase) %></h1>

<%= render "form", book: @comment.commentable, comment: @comment %>

<br>

<nav class="page-nav">
  <%= link_to t('views.common.back', name: i18n_pluralize(Book.model_name.human.downcase)), book_path(@comment.commentable) %>
</nav>
