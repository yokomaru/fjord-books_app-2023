<h1><%= t('views.common.title_new', name: Comment.model_name.human.downcase) %></h1>

<%= render "form", book: @book, comment: @comment %>

<br>

<nav class="page-nav">
  <%= link_to t('views.common.back', name: i18n_pluralize(Comment.model_name.human.downcase)), book_path(@book) %>
</nav>
